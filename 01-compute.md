# 01. 基盤モデルと Amazon Bedrock

この章で最も重要なのは、モデル名の暗記ではなく、要件に対して推論方式とモデルを選べることです。
モデルとリージョンの提供状況は変化が速いため、本番設計では必ず
[Models at a glance](https://docs.aws.amazon.com/bedrock/latest/userguide/models.html) を確認します。

## FM 選定の順序

1. 入出力モダリティ、言語、コンテキスト長、ツール利用、構造化出力などの必須能力で候補を絞る。
2. 実データに近い評価セットで、正確性・安全性・レイテンシー・トークン量を比較する。
3. リージョン、データ所在地、クォータ、利用条件、費用を確認する。
4. 一つのモデル ID をコードへ固定せず、設定またはルーティング層から切り替えられるようにする。

「最大・最新のモデル」が常に正解ではありません。単純な分類や抽出には小さいモデル、複雑な推論には高性能モデルを使うカスケードも有効です。

## API と推論方式

| 選択肢 | 向く要件 | 注意点 |
|---|---|---|
| Converse / ConverseStream | メッセージ対応モデルを共通形式で呼ぶ | モデル固有機能の対応状況を確認する |
| InvokeModel | モデル固有形式、画像・埋め込みなどを直接扱う | プロバイダー差をアダプター層へ隔離する |
| オンデマンド推論 | 変動する対話トラフィック、PoC | クォータとスロットリングを設計する |
| バッチ推論 | 即時性のない大量処理 | ジョブ状態、部分失敗、再処理を管理する |
| Provisioned Throughput | 継続的で予測可能な容量 | 利用率と契約条件を確認する |
| 推論プロファイル | クロスリージョン分散、利用量追跡 | ルーティング先リージョンと SCP を確認する |

Bedrock にはネイティブ API に加えてオープン互換 API もあります。試験では主に Converse、InvokeModel、ストリーミング、非同期処理の使い分けを判断できれば十分です。

## 可用性とデータ所在地

- **地理ベースのクロスリージョン推論**は、US、EU、APAC など指定された地理内で容量を利用します。
- **グローバルクロスリージョン推論**は、より広いリージョンへルーティングでき、所在地制約より可用性・コストを優先する場合の候補です。
- 推論プロファイルの全宛先リージョンを SCP が許可していないと、呼び出しが失敗する場合があります。
- クロスリージョン推論は自動的な容量ルーティングであり、アプリケーション全体の DR を代替しません。データ、依存 API、状態、復旧手順も設計します。

## モデルアクセスの現在の注意点

商用リージョンでは、必要な IAM と AWS Marketplace 権限があれば、モデルアクセスは原則デフォルトで有効です。
サードパーティーモデルの初回利用では自動サブスクリプションが行われ、モデルによっては追加条件や初回フォームがあります。
利用制限は「アクセス申請画面」だけに頼らず、SCP と IAM で `bedrock:InvokeModel` の対象を制御します。

## カスタマイズを選ぶ基準

- 最新知識や出典が必要なら、まず RAG を検討する。
- 文体、分類基準、安定した振る舞いの変更には、プロンプト改善やモデルカスタマイズを比較する。
- カスタムモデルには、データ権利、評価、Model Registry、段階展開、ロールバック、廃止までのライフサイクルが必要です。

## 試験での見分け方

- 「早く PoC」→ Bedrock のマネージド FM と代表データによる評価。
- 「同じコードで複数モデル」→ Converse API とモデル抽象化。
- 「高負荷が予測可能」→ Provisioned Throughput を利用率と費用で比較。
- 「リージョン障害・混雑」→ 推論プロファイルとグレースフルデグラデーション。
- 「最新の社内情報」→ モデルの再学習ではなく RAG。

## 公式リソース

- [Amazon Bedrock API の選択](https://docs.aws.amazon.com/bedrock/latest/userguide/apis.html)
- [モデルアクセス](https://docs.aws.amazon.com/bedrock/latest/userguide/model-access.html)
- [クロスリージョン推論](https://docs.aws.amazon.com/bedrock/latest/userguide/cross-region-inference.html)
- [AIP-C01 ドメイン 1](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain1.html)
