# 02. データ管理・RAG

RAG の品質はモデルよりも、データの品質・権限・鮮度・検索設計に左右されることがあります。取り込みから回答までを一つのライフサイクルとして設計します。

## 重要なデータフロー

1. S3、文書管理システム、データベースなどから承認済みデータを取得する。
2. 形式、必須項目、破損、重複、言語、PII、利用権利を検証する。
3. 文書構造に合わせてチャンク化し、出典・版・ACL・テナントなどのメタデータを付ける。
4. 埋め込みを生成してベクトルストアへ同期し、追加・更新・削除を追跡する。
5. 認証済み主体から検索フィルターを作り、検索結果と引用情報を FM へ渡す。
6. 検索品質、回答の根拠性、同期遅延を評価・監視する。

## Knowledge Bases を選ぶとき

Amazon Bedrock Knowledge Bases は、データソース、取り込み、チャンク、埋め込み、検索、引用付き生成をマネージドに統合します。次の場合に有力です。

- 標準的な RAG を短期間で構築したい。
- `Retrieve` と `RetrieveAndGenerate` を使い分けたい。
- Guardrails、再ランキング、メタデータフィルターを Bedrock 内で統合したい。

独自の複雑な ACL、特殊な前処理、複数検索エンジンの統合、検索アルゴリズムの細かな制御が必要なら、カスタムパイプラインを検討します。

## チャンク方式の判断

| 方式 | 長所 | 注意点 |
|---|---|---|
| 固定サイズ | 単純で予測しやすい | 見出し、表、手順を分断しやすい |
| セマンティック | 意味のまとまりを保ちやすい | 取り込み費用とデータ依存の評価が必要 |
| 階層型 | 小さい子チャンクで検索し、広い親文脈を返せる | 返却件数やメタデータ制約を確認する |
| カスタム変換 | 独自文書や ACL を精密に処理できる | 実装・運用責任が増える |

「最適なサイズ」は固定値ではありません。正解文書を持つ代表クエリで検索再現率、回答品質、トークン量を比較します。

## マルチモーダルと非構造データ

Amazon Bedrock Data Automation は、文書、画像、音声、動画から標準出力を生成できます。
文書・画像・音声では Blueprint により、必要なフィールドを定義したカスタム出力も作れます。
大量処理では非同期 API、出力スキーマ、Blueprint の版、抽出精度、失敗項目の再処理を管理します。

Data Automation は「非構造データの理解・抽出」、Knowledge Bases は「検索可能な知識として取得・生成」が中心です。抽出結果を検証してから Knowledge Base へ取り込む構成もあります。

## 鮮度とアクセス制御

- テナントや部署は、利用者入力ではなく認証済みクレームから導きます。
- プロンプトで「他社情報を出さない」と指示するだけでは認可になりません。
- 追加だけでなく更新・削除も同期し、文書版、最終同期時刻、失敗数を監視します。
- 引用にはソース URI、ページ、文書版など、利用者が確認できる情報を関連付けます。

## 試験での見分け方

- 「PDF の表や章が壊れる」→ 構造対応・階層型・セマンティックチャンクを評価。
- 「別テナントの文書が混じる」→ 検索と保存層でメタデータ ACL を強制。
- 「日々更新される」→ 差分同期、削除反映、鮮度メトリクス。
- 「画像・音声・文書から定型抽出」→ Bedrock Data Automation と Blueprint。

## 公式リソース

- [Knowledge Bases for Amazon Bedrock](https://docs.aws.amazon.com/bedrock/latest/userguide/knowledge-base.html)
- [Knowledge Bases のチャンク](https://docs.aws.amazon.com/bedrock/latest/userguide/kb-chunking.html)
- [Bedrock Data Automation](https://docs.aws.amazon.com/bedrock/latest/userguide/bda-how-it-works.html)
