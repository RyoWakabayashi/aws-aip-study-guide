# 03. ベクトルストアと検索

ベクトルストアは「保存先」だけでなく、検索方式、フィルター、規模、運用、既存データとの関係で選びます。試験では製品機能の細部より、要件に合う検索構成を判断することが重要です。

## 主な選択肢

| 選択肢 | 向く場面 | 主な注意点 |
|---|---|---|
| Knowledge Bases のマネージド構成 | RAG の取り込みと検索を素早く統合 | 対応リージョン、モデル、ベクトルストアを確認 |
| Amazon OpenSearch Service | ベクトル、キーワード、ハイブリッド検索を精密に制御 | シャード、インデックス、検索運用が必要 |
| Aurora PostgreSQL + pgvector | 既存 PostgreSQL データと整合性・SQL を活用 | 大規模 ANN の性能と接続管理を評価 |
| Amazon S3 Vectors | 大量ベクトルを低コストで管理する構成 | 対応機能とレイテンシー要件を確認 |
| 専用 DB + 別ベクトルストア | DynamoDB などの状態・ACL と検索を分離 | 二つのストア間の整合性を管理 |

## 検索パイプライン

1. 質問を正規化し、必要なら会話文脈を補って書き換える。
2. 複合質問はサブクエリへ分解し、略語は展開する。
3. 認可済みメタデータフィルターを適用する。
4. ベクトル検索、キーワード検索、またはハイブリッド検索で候補を得る。
5. 必要なら Bedrock の reranker などで候補を再順位付けする。
6. 重複を除き、トークン予算内の根拠を FM へ渡す。

ハイブリッド検索は、製品番号・固有名詞の完全一致と自然文の意味一致を両立したい場合に有効です。再ランキングは精度を高めますが、追加のレイテンシーと費用を伴います。

## 評価指標

- **Recall@k**: 正解文書が上位 k 件に含まれた割合。
- **MRR / nDCG**: 正解文書の順位やランキング全体の質。
- **フィルター精度**: 認可外・期限切れ文書が混入していないか。
- **p95 / p99 レイテンシー**: 平均では見えない遅い検索。
- **回答の忠実度**: 取得文書があっても FM が根拠どおり答えたか。

検索指標と回答指標を分けると、「正解文書を取れない」のか「取れた文書を正しく使えない」のかを切り分けられます。

## 埋め込み移行

異なる埋め込みモデルのベクトルは、次元数が同じでも同じ空間とは限りません。モデル変更時は次の順序を守ります。

1. 新モデルで全対象文書を再埋め込みし、並行インデックスを作る。
2. 同じ評価クエリで新旧の検索品質と性能を比較する。
3. 段階的に検索先を切り替え、旧インデックスをロールバック用に保持する。
4. 安定後に旧インデックスを廃止する。

## よくある誤り

- 類似度を認可判定に使う。
- 検索後にクライアント側だけで ACL を適用する。
- 候補数を増やせば必ず品質が上がると考える。
- 文書削除後も古いベクトルを残す。
- 検索設定と生成モデルを同時変更し、原因を比較不能にする。

## 公式リソース

- [Knowledge Bases の検索設定](https://docs.aws.amazon.com/bedrock/latest/userguide/kb-test-config.html)
- [Amazon S3 Vectors](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-vectors.html)
- [Amazon OpenSearch Service](https://docs.aws.amazon.com/opensearch-service/)
- [AIP-C01 ドメイン 1](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain1.html)
