# 04. API・エージェント・統合

## FM API 統合

- 同期呼び出しは短い対話、ストリーミングは応答体感を改善する UI、非同期は長時間・大量の処理に使い分けます。
- スロットリングには指数バックオフとジッター、タイムアウト、再試行上限を実装します。
- API Gateway、Lambda、SQS、Step Functions、EventBridge を用途に応じて組み合わせます。
- X-Ray または相当のトレースで、API 境界をまたぐ失敗とレイテンシーを追跡します。

## エージェントとツール

エージェントには、明確なツール定義、入力検証、最小権限、タイムアウト、停止条件、人間の承認を設けます。MCP を使う場合も、ツールサーバーの認可・監査・ネットワーク境界はアプリケーションと同様に設計します。

## エンタープライズ統合

既存システムとは API またはイベント駆動で疎結合にします。業務操作の実行前に権限とパラメータを検証し、破壊的操作には human-in-the-loop を置きます。
