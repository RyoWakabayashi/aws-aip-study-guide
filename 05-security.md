# 05. AI 安全性・セキュリティ・ガバナンス

## 安全な入出力

- Amazon Bedrock Guardrails などで、禁止トピック、有害コンテンツ、PII、拒否応答を設計する。
- プロンプトインジェクションを前提とし、外部文書を命令として扱わない。ツール呼び出しは常にアプリケーション側で検証する。
- 入力・出力フィルターは品質評価と併せて調整し、過剰拒否も測定する。

## データ保護

- IAM の最小権限、KMS による暗号化、VPC エンドポイント、Secrets Manager を基本にする。
- データ分類・保存期間・削除・リージョンを定義し、ログに機密データを残さない。
- テナント境界はアイデンティティだけでなく、検索メタデータとストレージのアクセス制御にも適用する。

## 責任ある AI

バイアス、有害性、説明可能性、透明性、異常な使用を評価対象に含めます。モデル・プロンプト・データセット・ガードレールの変更を記録し、監査可能にします。
