# 05. AI 安全性・セキュリティ・ガバナンス

生成 AI の安全性は Guardrails だけでは完成しません。通常のクラウドセキュリティ、モデル固有の入出力制御、業務ガバナンスを重ねる「多層防御」で考えます。

## 制御を置く場所

| 層 | 主な制御 |
|---|---|
| API 入口 | Cognito / IAM、WAF、レート制限、入力サイズ制限 |
| 推論前 | PII 検出、プロンプト攻撃検知、データと命令の分離 |
| 推論 | Guardrails、許可モデル、トークン上限 |
| 検索 | テナント ACL、メタデータフィルター、出典 |
| ツール | スキーマ検証、最小権限、決定論的ポリシー、承認 |
| 出力後 | 構造検証、機密情報マスク、根拠確認、拒否 |
| 運用 | CloudTrail、監査ログ、ドリフト・乱用・費用監視 |

## Amazon Bedrock Guardrails

Guardrails はモデル入力と応答へ一貫した安全ポリシーを適用します。主な機能にはコンテンツフィルター、拒否トピック、単語フィルター、機密情報フィルター、プロンプト攻撃検知、コンテキストグラウンディングなどがあります。

- Guardrail は IAM の代替ではありません。内容を評価しても AWS リソースの操作権限は制御できません。
- `ApplyGuardrail` を使うと FM 呼び出しと独立した箇所でも評価できます。
- コンテキストグラウンディングは、参照元に対する根拠性と質問への関連性を評価します。対応ユースケースとストリーミング時の挙動を確認します。
- Automated Reasoning checks は、自然言語ポリシーから抽出した形式論理に対して回答を検証する機能です。検出結果を返すため、遮断・再生成・確認要求はアプリケーションが判断します。
- DRAFT を直接本番へ使わず、正常・違反・境界ケースで評価して不変バージョンを参照します。

## プロンプトインジェクション

攻撃文字列を完全に除去できると考えないことが重要です。

1. システム命令、利用者入力、検索文書、ツール結果を明確に分離する。
2. 外部コンテンツを命令として信頼しない。
3. モデルが誘導されても越えられない IAM・Policy・業務認可を置く。
4. 機密ツールを必要時だけ公開し、引数と結果を検証する。
5. 直接・間接攻撃、jailbreak、情報抽出を継続的にテストする。

## データ保護

- 保存時は KMS、転送時は TLS、秘密値は Secrets Manager を使う。
- VPC エンドポイントと AWS PrivateLink は閉域経路を提供しますが、IAM を不要にはしません。
- ログはデフォルトで最小化し、必要な場合も PII をマスクしてアクセス・保持期間を分離します。
- データ所在地要件がある場合、クロスリージョン推論プロファイルの全宛先を確認します。
- テナント境界は認証、検索、ストレージ、キャッシュ、会話メモリのすべてへ適用します。

## ガバナンスと責任ある AI

- モデル、プロンプト、Guardrail、データ、評価結果、所有者、承認状態を資産台帳で追跡する。
- モデルカードには用途、制限、既知のリスク、評価結果を記載する。
- 正確性だけでなく、公平性、有害性、透明性、過信、利用者への影響をサブグループ別に評価する。
- AI の利用、出典、不確実性、制限を利用者に伝え、訂正・異議申立て経路を用意する。
- 高リスク判断はモデルを最終決定者にせず、人間または決定論的ルールを残す。

## 試験での見分け方

- 「有害な入出力」→ Guardrails。
- 「AWS API を呼べるか」→ IAM / AgentCore Policy。
- 「外部文書に悪意ある命令」→ データ分離とツール側の強制制御。
- 「規則への論理的一貫性」→ Automated Reasoning checks。
- 「監査・規制対応」→ 版、出典、主体、決定、承認を関連付けた証跡。

## 公式リソース

- [AIP-C01 ドメイン 3](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain3.html)
- [Amazon Bedrock Guardrails](https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails.html)
- [Automated Reasoning checks](https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails-automated-reasoning-checks.html)
