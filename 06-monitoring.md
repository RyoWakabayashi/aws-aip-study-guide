# 06. オブザーバビリティと運用

生成 AI では「API が成功した」だけでは正常と判断できません。運用指標、生成品質、安全性、費用、業務成果を同じリリース版へ関連付けます。

## 最小限の指標セット

| 領域 | 重要な指標 |
|---|---|
| 信頼性 | 呼び出し数、4xx / 5xx、スロットリング、再試行、タイムアウト、フォールバック率 |
| 性能 | TTFT、総レイテンシー、出力トークン毎秒、キュー時間、p50 / p95 / p99 |
| 利用量 | 入出力トークン、キャッシュ読み書き、同時実行、ツール呼び出し数 |
| RAG | Recall@k、検索空振り、再ランキング時間、同期遅延、引用率 |
| 品質・安全 | タスク成功、根拠性、形式遵守、拒否率、Guardrail 介入、誤検知 |
| 業務 | 解決率、有人転送、採用率、処理時間短縮、一件当たり総コスト |

平均だけでは、一部利用者に集中する遅延や品質事故を見逃します。分位点とモデル・プロンプト・テナント・機能・リージョンなどの低カーディナリティ属性で分析します。

## ログとトレース

- 相関 ID を API、検索、FM、エージェント、ツール、非同期ジョブへ伝播する。
- モデル ID、推論プロファイル、プロンプト版、Guardrail 版、検索設定版を記録する。
- Bedrock Model Invocation Logging はデフォルト無効です。有効化すると入力・出力も記録できるため、必要性、PII、保存先、保持期間を先に決めます。
- CloudTrail は管理・API 操作の監査、CloudWatch はメトリクス・ログ、X-Ray / OpenTelemetry はサービス境界のトレースに使います。
- AgentCore では Runtime、Memory、Gateway、ツールなどのメトリクスを CloudWatch で確認でき、詳細スパンやログは明示的な有効化が必要な場合があります。

## アラーム設計

単発エラーではなく、利用者影響へつながる複数の信号を使います。

- p95 TTFT とタイムアウト率が同時上昇 → 容量・下流依存を調査。
- トークン量と費用が基準から急増 → 乱用、プロンプト肥大、再試行ループを調査。
- 検索 Recall と根拠性が低下 → 同期、チャンク、埋め込み、フィルターを調査。
- Guardrail 介入率が急変 → 攻撃増加、利用分布またはポリシー版の変化を調査。

アラームには所有者、初動、縮退、ロールバック条件を関連付けます。通知だけで自動復旧になるわけではありません。

## 観測コストとプライバシー

- 本文を常時全量保存せず、メタデータ常時収集、正常時サンプリング、異常時詳細化を使い分ける。
- 利用者 ID やプロンプト全文をメトリクス次元にしない。
- デバッグログと監査ログの目的、権限、保持期間を分ける。
- S3 や CloudWatch Logs へ保存したデータも暗号化、削除要求、データ所在地の対象です。

## 試験での見分け方

- 「表示開始が遅い」→ TTFT。
- 「一部だけ遅い」→ p95 / p99 と入力トークン量。
- 「RAG のどこが遅い」→ 検索・再ランキング・推論を別スパンにする。
- 「費用増の原因」→ モデル・機能・テナント別のトークンと再試行を関連付ける。
- 「エージェント品質」→ セッション成功、ツール選択、軌跡、停止理由を観測する。

## 公式リソース

- [Bedrock ランタイムメトリクス](https://docs.aws.amazon.com/bedrock/latest/userguide/monitoring-runtime-metrics.html)
- [Model Invocation Logging](https://docs.aws.amazon.com/bedrock/latest/userguide/model-invocation-logging.html)
- [AgentCore Observability](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/observability.html)
