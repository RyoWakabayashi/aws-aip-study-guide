# 07. GenAI アーキテクチャパターン

設問ではサービス名より、データフロー、信頼境界、同期・非同期、状態管理を読み取ります。以下の五つを基本形として覚えます。

## 1. 認可付き RAG

`利用者 → 認証 → ACL 付き検索 → 再ランキング → FM → 根拠確認 → 引用付き応答`

- 認証主体からテナント・部署を導き、検索前にフィルターする。
- 文書更新・削除を差分同期し、出典版を応答へ含める。
- 検索品質と回答の忠実度を別々に評価する。

## 2. 承認付きエージェント

`利用者 → エージェント → ツール候補 → 入力検証・認可 → 承認 → 実行 → 監査`

- モデルはツールを提案できますが、実行権限を決定しません。
- 最大ステップ、時間・トークン予算、停止条件、冪等性を設ける。
- AgentCore Gateway / Identity / Policy またはアプリケーション層で強制境界を作る。

## 3. 非同期ドキュメント処理

`S3 → EventBridge / SQS → Step Functions → Data Automation / FM → 検証 → 保存 / 人手確認`

- 長時間処理を利用者の HTTP 接続から分離する。
- ジョブ・項目ごとの状態を保存し、DLQ と部分再処理を用意する。
- 抽出結果はスキーマ検証してから業務システムや Knowledge Base へ渡す。

## 4. GenAI ゲートウェイとモデルルーティング

`複数アプリ → 共通ゲートウェイ → 静的 / 動的ルーター → 複数 FM・推論プロファイル`

- 認証、許可モデル、利用上限、Guardrail、監査を共通化する。
- 単純要求は小型モデル、複雑な要求は高性能モデルへ送る。
- Bedrock Intelligent Prompt Routing は同じモデルファミリー内の対応モデルを品質予測で選択します。任意モデル間の業務ルールにはアプリ側ルーターを使います。

## 5. マルチエージェント

`オーケストレーター → 専門エージェント群 → 結果検証・集約`

- 各エージェントの責任、入出力契約、権限、委譲条件を明確にする。
- 循環委譲と重複実行を最大ホップ・所有権・冪等性で防ぐ。
- 個々の応答だけでなく、全体のタスク完了とツール軌跡を評価する。

## 横断的な設計原則

- **交換可能性**: モデル、プロンプト、検索、ツールを版付きの独立コンポーネントにする。
- **外部状態**: 会話・ジョブ状態をプロセスメモリだけに置かない。
- **有限の予算**: 時間、ステップ、トークン、費用、同時実行に上限を設ける。
- **決定論的な境界**: 認可、金額制限、スキーマ、承認をモデルへ委ねない。
- **劣化設計**: 代替モデル、読み取り専用、キャッシュ回答、人手転送を用意する。
- **評価可能性**: 構成版と相関 ID を残し、変更前後を比較できるようにする。

## よくある誤り

- Guardrails を IAM や業務認可の代わりにする。
- クロスリージョン推論だけでアプリ全体の DR が完成したと考える。
- マルチエージェント化すれば単一エージェントより必ず高品質だと考える。
- 同期チェーンを伸ばし続け、タイムアウトと再試行増幅を招く。

## 公式リソース

- [AWS Generative AI Lens](https://docs.aws.amazon.com/wellarchitected/latest/generative-ai-lens/welcome.html)
- [AIP-C01 ドメイン 2](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain2.html)
