# 07. GenAI アーキテクチャパターン

## 代表パターン

1. **RAG チャット**: UI → API → 認可付き検索 → Bedrock → 根拠付き応答。
2. **エージェント業務支援**: FM がツール候補を選択し、アプリケーションが認可・検証して実行。高リスク操作は承認フローへ。
3. **非同期文書処理**: S3 イベント → SQS / Step Functions → 抽出・分類・要約 → 保存・レビュー。
4. **モデルルーティング**: 要件によりモデルを選び、障害時には別モデル・別リージョンへフォールバック。

## 設計原則

- モデル・プロンプト・ツール・検索を独立した交換可能なコンポーネントにする。
- 品質、セキュリティ、コストを後付けにせず、要件と受入基準に含める。
- AI の回答を信頼境界の外側に置き、権限判定と副作用のある処理は決定論的なコードで実行する。
