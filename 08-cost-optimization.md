# 08. パフォーマンスとコスト最適化

最適化の目的は、モデル単価を下げることではなく、品質と SLO を満たす「成功タスク一件当たりの総コスト」を下げることです。

## 優先して検討するレバー

1. **不要な呼び出しを減らす**: 決定論的処理、事前計算、完全一致キャッシュを使う。
2. **不要なトークンを減らす**: 履歴要約、検索候補の選別、出力上限、簡潔な指示を使う。
3. **必要十分なモデルを使う**: タスク別ルーティングやモデルカスケードを評価する。
4. **同じ長い接頭辞を再利用する**: 対応モデルではプロンプトキャッシュを検討する。
5. **即時性で分ける**: 夜間処理はバッチ、長時間処理は非同期、UI はストリーミングを使う。
6. **容量方式を合わせる**: 実測したトークン処理量と利用率でオンデマンドと Provisioned Throughput を比較する。

## キャッシュを混同しない

| 種類 | 再利用するもの | 重要な注意点 |
|---|---|---|
| 応答キャッシュ | 完成した回答 | データ版、認可、モデル・プロンプト版をキーに含める |
| セマンティックキャッシュ | 意味が近い質問の回答 | 誤ヒット率を評価し、高リスク用途を除外する |
| プロンプトキャッシュ | 長く繰り返す入力接頭辞の計算 | 対応モデル、最小トークン、TTL、書込・読取費用を確認する |
| RAG | 最新データの検索結果 | キャッシュではなく、質問ごとに根拠を取得する仕組み |

プロンプトキャッシュは対応するオンデマンド推論で、静的な長い文脈を繰り返す場合に有効です。プロンプトの接頭辞が変わるとヒットしません。

## レイテンシーの分解

`総時間 = API 待ち + 検索 + 再ランキング + キュー + TTFT + 生成 + 後処理`

- TTFT 改善にはストリーミング、短い入力、レイテンシー最適化された推論を検討する。
- 独立処理は同時実行上限付きで並列化する。
- 検索はフィルター、候補数、インデックス、再ランキングを品質と一緒に調整する。
- タイムアウト後も処理を続けないよう、キャンセルを下流へ伝播する。

## 容量計画

HTTP リクエスト数だけでは不十分です。入力・出力トークン、生成時間、同時ストリーム、キュー長、再試行率で負荷を測ります。平均ではなくピークと p95 / p99 を使い、バックプレッシャーと主体別クォータを設けます。

## 総コストの式

`総コスト = 推論 + Guardrail + 検索 + 再ランキング + キャッシュ + ログ + 再試行 + 人手レビュー + 誤回答対応`

小型モデルで誤回答や有人転送が増えれば、モデル料金が安くても総コストは上がります。解決率、採用率、品質事故も一件当たり費用へ関連付けます。

## 試験での見分け方

- 「同じ長い指示を反復」→ プロンプトキャッシュ。
- 「同じ確定回答を反復」→ 応答キャッシュ。
- 「単純／複雑な要求」→ モデルルーティング。
- 「夜間に大量」→ バッチ推論。
- 「待ち時間の体感」→ ストリーミングと TTFT。
- 「安いモデルで手戻り増」→ タスク成功当たりの総コストで再評価。

## 公式リソース

- [AIP-C01 ドメイン 4](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain4.html)
- [Bedrock のプロンプトキャッシュ](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-caching.html)
- [Intelligent Prompt Routing](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-routing.html)
