# 09. テスト・評価・障害対応

生成 AI のテストでは、同じ入力でも表現が変わることを前提にします。文字列一致だけでなく、タスク別の評価、検索と生成の分離、重要ケースの合否条件が必要です。

## 評価セットの構成

- 通常利用を代表する質問と本番の主要言語・入力長。
- 空入力、長文、曖昧さ、誤字、複数意図などの境界ケース。
- プロンプトインジェクション、jailbreak、PII、権限外操作などの敵対ケース。
- 過去の障害・低評価・人手エスカレーションを再現する回帰ケース。
- 頻度は低くても重大な損害を生むケース。

データは版管理し、PII を除去し、重複・ラベル品質・学習データとの汚染を確認します。

## 対象別の評価

| 対象 | 主な指標 |
|---|---|
| モデル | 正確性、関連性、指示遵守、構造、頑健性、安全性 |
| RAG 検索 | Recall@k、MRR / nDCG、レイテンシー、ACL 違反 |
| RAG 生成 | 忠実度、回答正確性、引用一致、適切な回答拒否 |
| エージェント | タスク完了、正しいツール、引数、軌跡、ステップ数、費用 |
| 本番 UX | 解決率、採用率、有人転送、利用者フィードバック |

Amazon Bedrock Evaluations は、モデルと Knowledge Base / RAG を自動・LLM-as-a-judge・人手で評価できます。AgentCore Evaluations は、エージェントのセッション、トレース、ツール呼び出しに対するオンライン・オンデマンド・バッチ評価に利用できます。

## LLM-as-a-judge の注意点

- 評価基準とスケールを具体的に定義する。
- 順序バイアス、自己選好、言語・カテゴリ別の偏りを確認する。
- 専門家ラベルとの一致度を測り、定期的に再校正する。
- 高リスクケースは単一の judge に任せず、人手または決定論的検査を併用する。
- 生成モデル、judge、評価プロンプト、データセットの版を記録する。

## トラブルシューティングの順序

1. 相関 ID から失敗した層と構成版を特定する。
2. 認証、IAM、モデルアクセス、リージョン、クォータ、入力形式を確認する。
3. コンテキスト切り捨て、最大トークン、タイムアウト、再試行を確認する。
4. RAG は「取り込み → フィルター → 検索 → 再ランキング → 生成」の順に分ける。
5. エージェントはツール選択、引数、認可、結果、停止条件を軌跡から確認する。
6. 一要因ずつ変更し、複数回試行で発生率を比較する。
7. 安全な版へ戻し、失敗例を回帰テストへ追加する。

## DR とゲームデー

- RTO / RPO と、劣化時に許容する品質・機能を定義する。
- モデル・リージョンだけでなく、データ、ベクトルストア、認証、外部ツールを対象にする。
- クロスリージョン推論の宛先とデータ所在地要件を確認する。
- 制御された障害注入で IAM、データ同期、切替、ロールバックを実際に通す。
- 影響範囲、停止条件、担当者、利用者連絡を事前に決める。

## 試験での見分け方

- 「検索結果に正解文書がない」→ Recall@k、チャンク、埋め込み、フィルター。
- 「文書は取れたが誤回答」→ 忠実度、プロンプト、出力検証。
- 「自動評価を信用できない」→ 人手ラベルで judge を校正。
- 「新モデルの安全な比較」→ 固定評価＋カナリア＋自動ロールバック。
- 「たまに再現する」→ 構成スナップショットと反復試行。

## 公式リソース

- [AIP-C01 ドメイン 5](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain5.html)
- [Amazon Bedrock Evaluations](https://docs.aws.amazon.com/bedrock/latest/userguide/evaluation.html)
- [AgentCore Evaluations](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/how-it-works-evaluations.html)
