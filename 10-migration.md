# 10. デプロイと継続的改善

GenAI のリリース単位はアプリコードだけではありません。モデル、プロンプト、検索、データ、Guardrail、ツールを一つの再現可能な構成として扱います。

## 版管理するもの

| 対象 | 記録する情報 |
|---|---|
| モデル | モデル / 推論プロファイル ID、推論設定、利用リージョン |
| プロンプト | Prompt Management の不変版、変数、承認 |
| RAG | 埋め込み、チャンク、インデックス、再ランキング、データ版 |
| 安全性 | Guardrail と Automated Reasoning ポリシーの版 |
| エージェント | ツール定義、権限、Policy、最大ステップ、メモリ設定 |
| 評価 | データセット、採点器、閾値、結果、例外承認 |

## Prompt Management と Flows

Prompt Management は、変数を持つ再利用可能なプロンプト、バリアント、バージョンを管理します。本番では編集可能な下書きではなく、評価・承認済みの固定版を参照します。

Bedrock Flows はプロンプト、Knowledge Base、Lambda などをつなぎ、不変のフロー版とエイリアスで展開できます。フローのノードだけでなく、参照するプロンプト・モデル・権限も同じリリースとして追跡します。

## CI/CD の品質ゲート

1. IaC、コード、ポリシー、スキーマの静的検査を行う。
2. 固定評価セットで品質、安全性、RAG、ツール動作を回帰テストする。
3. レイテンシー、トークン、費用の上限を確認する。
4. 高リスク変更に承認を要求する。
5. シャドー、カナリア、段階展開で版別メトリクスを比較する。
6. 閾値超過時は自動的に既知の安全な版へ戻す。

平均点が高くても、重大な安全ケースや必須業務ケースが失敗したらリリースを止めます。

## 変更別の移行戦略

- **モデル変更**: 共通 API / アダプターで差を隔離し、品質・形式・安全・コストを比較する。
- **埋め込み変更**: 並行インデックスへ再埋め込みし、検索先を段階切替する。
- **チャンク変更**: 新しい取り込み版を作り、正解文書の取得率を比較する。
- **Guardrail 変更**: 違反阻止率と正常入力の誤検知率をカナリア評価する。
- **ツール変更**: 契約テスト、最小権限、冪等性、承認経路を検証する。
- **AgentCore 変更**: セッション、メモリ、Gateway、Identity、Policy の互換性とテレメトリを確認する。

## 継続的改善ループ

`観測 → 失敗分類 → 仮説 → オフライン評価 → 段階展開 → 本番 KPI → 評価セット更新`

利用者フィードバックはそのまま正解ではありません。同意・代表性・重複・PII を確認し、検索、プロンプト、UI、ツール、モデルのどこに原因があるかを切り分けます。

## よくある誤り

- 「最新版」を本番が自動参照し、再現できない。
- モデルとプロンプトを同時に変え、改善要因が分からない。
- 旧インデックスや旧 Guardrail を先に削除して戻せない。
- オフライン評価だけで、本番の解決率・有人転送を確認しない。
- 一時的な緩和策を恒久対策として残す。

## 公式リソース

- [Amazon Bedrock Prompt Management](https://docs.aws.amazon.com/bedrock/latest/userguide/prompt-management.html)
- [Amazon Bedrock Flows](https://docs.aws.amazon.com/bedrock/latest/userguide/flows.html)
- [AIP-C01 ドメイン 2](https://docs.aws.amazon.com/ja_jp/aws-certification/latest/ai-professional-01/ai-professional-01-domain2.html)
