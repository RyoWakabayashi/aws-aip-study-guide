# 12. キーワード対応表

| 設問のキーワード | まず考えること | 避ける誤答 |
|---|---|---|
| 最新の社内文書、出典 | RAG、同期、引用、データ版 | 再学習だけで更新する |
| 別テナントが混入 | 認証由来のメタデータ ACL、保存層認可 | プロンプトだけで分離する |
| PDF の章・表が分断 | セマンティック／階層型／カスタムチャンク | 全文書を一チャンクにする |
| 製品番号と自然文 | ハイブリッド検索、再ランキング | ベクトル検索だけに固定する |
| 埋め込みモデル変更 | 再埋め込み、並行インデックス、段階切替 | 新旧ベクトルを混在させる |
| 文書・画像・音声から抽出 | Bedrock Data Automation、Blueprint、スキーマ検証 | 無検証で業務 DB へ保存する |
| 同じコードで複数 FM | Converse、アダプター、設定外部化 | モデル ID を全コードへ固定する |
| 応答を逐次表示 | ストリーミング、SSE / WebSocket、TTFT | 完了まで接続を無反応にする |
| 長時間・大量処理 | SQS、Step Functions、バッチ、ジョブ状態 | 同期 API のタイムアウトだけ延長する |
| 再配信で二重実行 | 冪等性キー、条件付き書き込み | 再試行をモデルへ判断させる |
| ツールを標準接続 | MCP、サーバー側認可、入力検証 | MCP が認可も保証すると考える |
| 任意フレームワークのエージェント運用 | AgentCore Runtime / Gateway / Identity / Observability | AgentCore を FM とみなす |
| 高リスク操作 | 最小権限、Policy、承認、監査 | モデル出力をそのまま実行する |
| 不適切な入出力 | Guardrails、入力／出力評価、版テスト | IAM だけで内容も安全になると考える |
| 規則への論理的一貫性 | Automated Reasoning checks、検出後のアプリ処理 | 自動的に必ず遮断されると考える |
| モデル停止・混雑 | 推論プロファイル、回路遮断、劣化応答 | 無制限に即時再試行する |
| データ所在地 | 地理 CRIS の宛先、SCP、ログ・データ配置 | グローバル CRIS を無条件に使う |
| 同じ長い指示を反復 | プロンプトキャッシュ | 完成回答キャッシュと混同する |
| 一部だけ遅い | p95 / p99、入力トークン、段階別トレース | 平均レイテンシーだけを見る |
| RAG の品質低下 | 取り込み→検索→再ランキング→生成を分離評価 | 全設定を同時変更する |
| 自動評価が不一致 | 人手ラベルで judge を校正 | judge の点数を絶対視する |
| 安全なモデル更新 | 固定評価、カナリア、版別監視、ロールバック | 最新版を全量へ直接適用する |

## 判断の型

1. 品質、性能、コスト、セキュリティ、運用のどの要件かを特定する。
2. モデル判断でよい部分と、コード・IAM・ポリシーで強制する部分を分ける。
3. 同期／非同期、ステートレス／状態あり、単一／複数リージョンを整理する。
4. 変更を評価・観測・ロールバックできる選択肢を優先する。
