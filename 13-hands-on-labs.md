# 13. ハンズオン演習

> 実行前に料金、対応リージョン、IAM、削除手順を確認してください。商用リージョンのモデルアクセスは原則デフォルト有効ですが、サードパーティーモデルの初回利用には AWS Marketplace 権限や追加条件が必要な場合があります。機密データは使用しないでください。

## Lab 1: 共通 API とストリーミング

1. 同じテスト入力を Converse と ConverseStream で呼び出す。
2. TTFT、総時間、入出力トークン、モデル ID を構造化ログへ記録する。
3. タイムアウト、スロットリング、再試行上限、キャンセルを試す。

**確認ポイント**: モデル固有コードが分離され、途中切断を不完全な成功として扱っていないこと。

## Lab 2: RAG とアクセス制御

1. 部署 A / B のテスト文書を S3 から Knowledge Base へ取り込む。
2. 固定、セマンティック、または階層型チャンクのうち二つを比較する。
3. 認証済み部署を想定したメタデータフィルターを強制する。
4. 10件以上の正解文書付き質問で Recall@k と回答の忠実度を測る。

**確認ポイント**: プロンプトを変更しても他部署文書を取得できず、引用から文書版を確認できること。

## Lab 3: 安全なツール利用

1. 読み取り専用ツールと、架空の高リスク更新ツールを定義する。
2. JSON Schema、IAM 最小権限、冪等性キー、最大ステップを設定する。
3. 高リスク操作を Step Functions の承認へ送る。
4. 直接・間接プロンプトインジェクションを入力し、実行が阻止されることを確認する。

**確認ポイント**: モデルが不正なツールを選んでも、アプリケーション側の認可で止まること。

## Lab 4: Guardrail と評価

1. 正常、禁止トピック、PII、プロンプト攻撃の評価セットを作る。
2. Guardrail の DRAFT を入力・出力または `ApplyGuardrail` で評価する。
3. 見逃し率と正常入力の誤検知率を比較して閾値を調整する。
4. 合格した版を作り、本番参照を想定して固定する。

**確認ポイント**: IAM の拒否と Guardrail の介入が別の制御であることをログから説明できること。

## Lab 5: 継続評価とカナリア

1. プロンプトまたはモデルの現行版と候補版を用意する。
2. 正確性、根拠性、形式、安全性、TTFT、費用を同じ評価セットで比較する。
3. LLM-as-a-judge の一部を人手採点し、一致度を確認する。
4. 10% カナリアを想定し、品質または安全性の閾値超過で旧版へ戻す。

**確認ポイント**: モデル、プロンプト、評価セット、judge の版から結果を再現できること。

## 終了時チェック

- Knowledge Base、ベクトルストア、S3 オブジェクト、ログ、評価結果を削除または保持期限設定したか。
- Provisioned Throughput、AgentCore Runtime、OpenSearch など継続課金するリソースが残っていないか。
- サンプル IAM にワイルドカード権限や長期アクセスキーが残っていないか。
