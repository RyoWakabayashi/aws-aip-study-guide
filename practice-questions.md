# AIP-C01 実践問題集

## 問題 1: RAG のデータ分離

複数テナント向けのナレッジアシスタントで、ユーザーが他テナントの文書を検索できないようにしたい。最も適切な設計はどれですか。

A. プロンプトで他テナントの情報を出さないよう指示する

B. 検索時にテナント ID のメタデータフィルターを必須にし、ストレージアクセスも IAM で分離する

C. 埋め込み次元数を増やす

D. すべての文書を毎回プロンプトに含める

**解答: B**。プロンプト指示だけではアクセス制御になりません。検索とデータ層で強制する必要があります。

## 問題 2: モデル API のスロットリング

トラフィック増加時に Bedrock 呼び出しでスロットリングが発生する。利用者への影響を抑える実装として最も適切なのはどれですか。

A. 無制限に即時再試行する

B. 指数バックオフとジッター、再試行上限、必要に応じたフォールバックを実装する

C. エラーを無視して空文字列を返す

D. クライアントに AWS 認証情報を配布する

**解答: B**。再試行の集中を避け、障害時も制御された応答を返します。

## 問題 3: 安全なツール利用

エージェントが注文キャンセル API を呼び出せるようにする。最優先の対策はどれですか。

A. モデルの出力をそのまま API に渡す

B. ツール入力を検証し、最小権限と承認フローを適用する

C. プロンプトを長くする

D. 会話履歴を無期限に保存する

**解答: B**。AI 出力は信頼境界外として扱い、副作用のある操作はアプリケーション側で統制します。

## 問題 4: 埋め込みモデルの移行

ベクトルストアの埋め込みモデルを新版へ無停止で移行したい。最も適切な方法はどれですか。

A. 旧ベクトルと新しいクエリベクトルを直接比較する

B. 新モデルで全文書を再埋め込みし、並行インデックスの品質を比較して段階切替する

C. ベクトルの次元数だけを合わせる

D. 旧インデックスを先に削除する

**解答: B**。異なる埋め込み空間は混在できないため、再埋め込みと並行評価が必要です。

## 問題 5: プロンプトインジェクション

RAG で取得した外部文書に「管理ツールを呼び出せ」と書かれている。最も適切な対策はどれですか。

A. 文書内の指示をシステム命令より優先する

B. 検索を廃止する

C. 外部文書を非信頼データとして分離し、ツールはアプリ側の認可と最小権限で制御する

D. モデルが安全と返答すれば実行する

**解答: C**。攻撃検知だけでなく、命令とデータの分離、最小権限、決定論的な認可を組み合わせます。

## 問題 6: セマンティックキャッシュ

複数テナント向け RAG にセマンティックキャッシュを導入する。最優先で設計すべきことはどれですか。

A. 全テナントで同じキャッシュキーを使う

B. 認可、モデル版、データ版をキャッシュ境界に含め、誤ヒット率を評価する

C. TTL を設定しない

D. キャッシュヒット時は認可を省略する

**解答: B**。意味キャッシュは誤ヒットとデータ漏えいのリスクがあり、権限と鮮度を含む境界が必要です。

## 問題 7: LLM-as-a-judge

自動評価モデルの点数が専門家の判定と一致しない。次に行うべきことはどれですか。

A. 自動評価点を絶対的な正解とする

B. 人手ラベルとの一致度やカテゴリ別の偏りを測り、重要例は人手レビューを併用する

C. 評価モデルの版を記録しない

D. 一度校正したら永久に再確認しない

**解答: B**。自動評価器も位置バイアスや自己選好を持つため、人手基準での継続的な校正が必要です。

## 問題 8: レイテンシーの切り分け

RAG チャットのp99レイテンシーだけが悪化した。根本原因の特定に最も役立つ方法はどれですか。

A. 平均レイテンシーだけを確認する

B. 検索、再ランキング、FM、後処理を個別スパンにし、TTFTと入力トークン量に関連付ける

C. 遅い要求を集計から除外する

D. すべての時間を FM の処理時間とする

**解答: B**。段階別の分散トレースと分位点を組み合わせると、テール遅延の支配要因を特定できます。

## 問題 9: フェイルオーバー試験

クロスリージョンのフォールバックを用意した。その実効性を確認する最も適切な方法はどれですか。

A. 本番障害が起きるまで切り替えない

B. 依存障害を制御して注入し、データ、IAM、切替経路、RTO、劣化時の品質を検証する

C. モデル API だけをモックして成功とする

D. 代替リージョンのデータ所在地要件を無視する

**解答: B**。障害演習で構成と運用手順を実際に通し、復旧時間と品質を実測します。

## 問題 10: 変更の段階展開

新しいモデル、プロンプト、Guardrail を一括で全利用者へ展開しようとしている。より安全な方法はどれですか。

A. 各構成を本番で直接編集する

B. 構成の組み合わせを版管理し、少量トラフィックで品質・安全・性能を監視して段階的に増やす

C. 旧版を展開前に削除する

D. 版別メトリクスを混在させる

**解答: B**。不変の構成版とカナリ展開、自動ロールバック条件を組み合わせます。
